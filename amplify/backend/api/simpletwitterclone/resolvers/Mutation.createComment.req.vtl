#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createCommentInput.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createCommentInput[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO Comment $colStr VALUES $valStr", "SELECT * FROM Comment WHERE id=$ctx.args.createCommentInput.id"]
}