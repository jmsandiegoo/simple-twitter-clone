#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createTweetInput.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createTweetInput[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO Tweet $colStr VALUES $valStr", "SELECT * FROM Tweet WHERE id=$ctx.args.createTweetInput.id"]
}